{
  "name": "agentic-loop-monorepo",
  "version": "0.0.1",
  "description": "The agentic-loop plugins (OpenCode + Claude Code) and their shared core",
  "license": "MIT",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/core",
    "plugins/opencode",
    "claude-plugin/mcp-server"
  ],
  "scripts": {
    "prepare": "npm run build -w @agentic-loop/core && npm run build -w agentic-loop-mcp",
    "test:hooks": "node --test claude-plugin/hooks/*.test.mjs",
    "typecheck:all": "npm run typecheck -ws --if-present",
    "test:all": "npm run test -ws --if-present && npm run test:hooks"
  }
}
