{
  "name": "@agentic-workflow/core",
  "version": "0.0.1",
  "description": "Shared core for the agentic-workflow plugins: pure workflow state machine, task backlog store, git isolation, snapshots, verdicts, metrics",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "files": [
    "dist",
    "workflows"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test src/workflow/*.test.ts src/task/*.test.ts src/manifest/*.test.ts src/source/*.test.ts src/scheduler/*.test.ts src/*.test.ts"
  },
  "dependencies": {
    "undici": "^6.21.0",
    "yaml": "^2.9.0",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^5.6.0"
  }
}
