Goal: {{goal}}
---
{{#artifacts.assess}}The review to post:
{{artifacts.assess}}{{/artifacts.assess}}
---
{{#git}}{{#platform.github}}Post the review as exactly ONE comment: `gh pr comment <n> --body "…"`, opening with a one-line note that this is an automated first-pass review and the human reviewer stays the reviewer of record. NEVER approve, request changes, merge, close, or push; those stay a human call.{{/platform.github}}{{#platform.ado}}Post the review as exactly ONE new thread{{#platform.adoAccess.rest}} via the Azure DevOps REST API, authenticated with `curl -sS -u :"$AZURE_DEVOPS_EXT_PAT"`: `POST _apis/git/repositories/<repoId>/pullRequests/<n>/threads?api-version=7.1` with body `{"comments":[{"content":"…","commentType":"text"}],"status":"active"}`{{/platform.adoAccess.rest}}{{#platform.adoAccess.az}} via the `az` CLI: `az devops invoke --area git --resource pullRequestThreads --route-parameters project=<project> repositoryId=<repoId> pullRequestId=<n> --http-method POST --in-file thread.json --api-version 7.1` where `thread.json` is `{"comments":[{"content":"…","commentType":"text"}],"status":"active"}`{{/platform.adoAccess.az}}{{#platform.adoAccess.mcp}} via your connected Azure DevOps MCP server's thread/comment tool (e.g. microsoft/azure-devops-mcp; if no such tool is available, record an ERROR verdict naming the missing capability){{/platform.adoAccess.mcp}} — opening with a one-line note that this is an automated first-pass review and the human reviewer stays the reviewer of record. NEVER vote, approve, complete, abandon, or push; those stay a human call.{{/platform.ado}}{{/git}}
---
{{#worktree}}{{worktree.instructions}}{{/worktree}}
