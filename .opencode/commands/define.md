---
description: Enter the DEFINE stage of the agentic loop — turn a raw goal into a short spec the PLAN stage builds on
agent: define
subtask: true
---

Run the **DEFINE** stage of the agentic engineering loop
(define → plan → build → verify → review → ship) on:

**$ARGUMENTS**

Delegated to the read-only `define` subagent, which turns the goal into a
short spec (problem, goals, non-goals, acceptance boundaries) — not a
`SPEC.md` file, just loop context threaded into the next stage. Relay the
spec and stop — the next stage (plan) takes over.
