English | [繁體中文](README.zh-TW.md)

# Agentic loop — engineering workflow improvement plans

**Every plan here — 01–48 — is implemented and tested**, living in the shared
`@agentic-workflow/core` package (`packages/core/`) consumed by both the
OpenCode plugin and the Claude MCP server. They are kept as the design record
for those features, not as a pending backlog. Plans 10–13 shipped on
2026-08-02; plan 14 on 2026-08-03; plan 15 on 2026-08-07; plans 16–18 on
2026-08-08; plans 24–27 on 2026-08-11; plan 32 on 2026-08-17; plans 33–40 on
2026-08-23; plans 41–45 on 2026-08-26; plans 46–48 on 2026-09-02.

Sourced from: the current code (all cited paths and function names verified
against source at time of writing), the residual risks in
[`../threat-model.md`](../threat-model.md), and the documented limitations in
`README.md` / `skills/workflow-orchestration/SKILL.md`.

## The plans (all shipped)

| # | Plan | What it bought | Where it lives now |
|---|------|----------------|--------------------|
| 01 | [Worktree isolation](./01-worktree-isolation.md) | Human's checkout never touched; safe concurrent watch sessions in one instance | `packages/core/src/workflow/git.ts`, `ensureIsolation` in `packages/core/src/workflow/isolate.ts`, edit-guard in `plugins/opencode/src/index.ts`; `git.test.ts` |
| 02 | [State persistence](./02-state-persistence.md) | Crash/restart resumes at the exact stage with artifacts, not a re-plan | `packages/core/src/workflow/persist.ts`; `persist.test.ts` |
| 03 | [Ship + status commands](./03-ship-and-status-commands.md) | Audited `in-review → completed` move; backlog dashboard | `/agent-loop ship` + status in `plugins/opencode/src/workflow/driver.ts`, `summarizeBacklog` in `packages/core/src/task/store.ts`; `store.test.ts` |
| 04 | [Verdict quality](./04-verdict-quality.md) | Structured failure reasons feed re-builds; optional multi-lens review | `packages/core/src/workflow/verdict.ts`; `verdict.test.ts` |
| 05 | [Secret redaction](./05-secret-redaction.md) | Secrets scrubbed from durable artifacts before write | `packages/core/src/task/redact.ts`, wired in `packages/core/src/task/store.ts`; `redact.test.ts` |
| 06 | [Run metrics](./06-run-metrics.md) | Per-run stage timings + verdict history in the run log | `packages/core/src/workflow/metrics.ts`; `metrics.test.ts` |
| 07 | [Multi-workflow kinds on a common scheduler](./07-multi-workflow-scheduler.md) | One scheduler drives many workflow kinds (engineering + PR sitter); `@agentic-workflow/core` extracted so both plugins share one implementation | `packages/core/src/manifest/` (schema, registry, template), `packages/core/src/scheduler/` (scheduler, lease), `packages/core/src/source/` (backlog, github-pr, ado-pr, ledger); `workflows/engineering/`, `workflows/pr-sitter/` |
| 08 | [Deterministic check commands](./08-deterministic-gate-commands.md) | Declared test/typecheck/lint commands run driver-side; their exit codes are established fact for the check stage and floor its verdict, replacing the self-reported "tests are green" | `packages/core/src/workflow/checks.ts`, `checks` on `StageDefSchema` in `manifest/schema.ts`, `stageChecks`/`checksFor`/`unknownStageCheckKeys` in `config.ts` (and `SHELL_BEARING_WORKFLOW_KEYS`), `withCheckResults` in `workflow/engine.ts`, the fire-boundary run + finalization floor in both hosts; `checks.test.ts` |
| 09 | [Context budgets for stage prompts](./09-context-management.md) | Prompts stop growing monotonically across iterations: the plan artifact no longer accretes audit notes (nor serves a stale plan after a replan), per-stage character ceilings clamp what a stage reads while never trimming the structured verdict block or the stage contract, a bounded attempts ledger stops a weak model re-trying a fix that already failed, and prompt size is visible per stage in the hub | `packages/core/src/workflow/budget.ts`, `contextFor`/`unknownStageContextKeys` in `config.ts`, `extractPlan` in `task/store.ts`, the seam + ledger in `workflow/engine.ts`, `promptSize` in `packages/hub/src/server/metrics/prompt.ts`; `budget.test.ts` |
| 10 | [Replan-reason threading](./10-replan-reason-threading.md) | The plan gate's rejection reason reaches the next PLAN pass as a structured prompt section (parsed back off the audit note, retired automatically once a newer plan lands) instead of "dig through the audit notes"; multi-line reasons flattened to the single-line audit shape | `PLAN_REJECTED_MARKER`/`extractReplanReason` in `packages/core/src/task/store.ts`, `oneLineReason` in `workflow/gate.ts`, `replan` on `WorkflowState`, threading in `workflow/orchestrate.ts` + `workflow/engine.ts`, section in `workflows/engineering/stages/plan.md`; `store.test.ts`, `gate.test.ts`, `orchestrate.test.ts`, `engine.test.ts` |
| 11 | [Iteration budget in the stage prompts](./11-iteration-budget-prompts.md) | A re-fired BUILD is told "iteration N of M" and the final iteration is warned that a check failure now stops the loop for human re-planning; VERIFY's last pass is told its FAIL text is what the replan gate reads. `iterationCap` is the single resolution both the stop decision and the prompt use, so the two can never drift | `iterationCap` + the `iterations` context key in `packages/core/src/workflow/engine.ts`, sections in `workflows/engineering/stages/build.md`/`verify.md`; `engine.test.ts` |
| 12 | [The plan contract](./12-plan-contract.md) | PLAN's prompt mechanically carries the plan-structure contract (steps naming files, `### Verification` mapping each acceptance criterion to its proof, `### Out of Scope`), and the park gate refuses a plan with no Verification subsection — tolerant heading match, claim released, task stays queued | `planContract` on `StageDefSchema` (`manifest/schema.ts`), `planContractBlock`/`hasVerificationSection` in `workflow/verdict.ts`, compose branch in `workflow/engine.ts`, park veto in `workflow/terminal.ts`; `schema.test.ts`, `engine.test.ts`, `terminal.test.ts` |
| 13 | [Opt-in plan visualization](./13-plan-visualization.md) | With `workflows.<kind>.planVisualization: true`, PLAN's prompt asks for mermaid diagram(s) in the plan when the change's shape warrants it (state/lifecycle, cross-package flow, concurrency, data-shape) — agent-judged, never gate-enforced — and the hub's plan-review view renders the fence as a diagram in a sandboxed iframe, still commentable per block | `planVisualization` on `StageDefSchema` (`manifest/schema.ts`), `planVisualizationBlock` in `workflow/verdict.ts`, `planVisualizationFor` + config knob in `config.ts`, compose tail in `workflow/engine.ts`, `MermaidBlock.tsx`/`mermaid-embed.ts` in `packages/hub`; `schema.test.ts`, `config.test.ts`, `verdict.test.ts`, `engine.test.ts`, `mermaid-embed.test.ts` |
| 14 | [Symmetric stage context](./14-symmetric-stage-context.md) | The context plans 09–11 gave one stage now reaches its siblings: REVIEW sees what VERIFY established (the recorded verdict seam via the new `verdicts.<stage>` key, never the transcript) and gets the final-iteration warning; VERIFY sees the attempts ledger so a recurring failure reads as recurrence; a re-fired BUILD is pointed at its prior iterations' cumulative diff; the inlined check feedback and build summary carry data-not-instructions fences; plan.md drops its unrenderable worktree section | The `verdicts` context key in `packages/core/src/workflow/engine.ts`, sections + fences in `workflows/engineering/stages/*.md`, oracle mirrors + `PRIOR_WORK_SECTION`/`VERDICTS_SECTION` strips; `engine.test.ts` |
| 15 | [The unassessed-axis policy](./15-unassessed-axis-policy.md) | A minority axis `ERROR` with no blocking finding — the "could not assess" escape hatch the review contract itself invites — is now non-blocking: `effectiveVerdict` skips it, it satisfies coverage, and it rides into the next prompt as an "unassessed" section instead of stopping the run as a fake environment error that stranded the task. A declared PASS whose *every* axis is unassessed is still refused (worsened to ERROR at finalization), and an ERROR axis carrying findings keeps today's onError routing | `axisUnassessed`/`withUnassessedGuard` + the `effectiveVerdict` skip in `workflow/verdict.ts`, `finalizeCheckRecord` in `workflow/checks.ts`, host swaps in the OpenCode driver + Claude MCP server, contract text in `verdictContractBlock` + `prompts/agents/workflow-review/body.md`; `verdict.test.ts`, `checks.test.ts` |
| 16 | [Building on the checked-out branch](./16-current-branch-mode.md) | A new `taskBranch` config key names the branch the engineering loop works on: a prefix (default `"feature/"`, reproducing the old hard-coded name) or `false` — cut nothing and build in the main tree on the branch already checked out, for the human who is already on the branch this work belongs on. `base` becomes HEAD's sha there (a branch-name base would make the review diff empty), gated by the new `GitRef.onCurrentBranch` discriminant; worktrees are forced off, a start on the default branch is refused, and one run per working tree is enforced across processes | `taskBranchFor`/`taskBranchPrefix`/`worktreesDirFor` in `config.ts`, the current-branch arms + `assertNotDefaultBranch` + the lock in `workflow/isolate.ts`, `headSha`/`defaultBranchName`/`gitCommonDir` in `workflow/git.ts`, `extractRunBranch` in `task/store.ts` feeding `workflow/ship-pr.ts`, `git.cut`/`git.current` in `workflow/engine.ts` + the three engineering templates; `current-branch.git.test.ts`, `isolate.test.ts`, `git.test.ts`, `config.test.ts`, `store.test.ts`, `ship-pr.test.ts` |
| 17 | [Replan chains the re-plan](./17-replan-chains-plan.md) | A rejected plan no longer idles in `queued/`: `replanTask` stamps the task plan-next (the existing plan-request marker, `source: "replan"`), OpenCode's replan chains straight into the `plan <id>` claim + drive, and Claude/Qwen's replan becomes a hybrid verb whose continued turn runs exactly one PLAN pass — the revised plan re-parks in `plan-review/` with the rejection reason threaded in. The hub, which never drives a stage, gets the plan-next ordering; a replan aimed at an already-queued task records the fresh reason unless a planner holds its claim right now | `markPlanNext`/`replanQueued` + `data.id` in `workflow/gate.ts`, `source` on `requestPlan` in `task/plan-request.ts`, `claimForPlan` + chained `handleReplan` in the OpenCode driver, `continueTurn` in `plugins/claude/hooks/gate-parse.mjs`, the rewritten replan verb block in `prompts/verbs/engineering.md`; `gate.test.ts`, `plan-request.test.ts`, `driver.test.ts`, `gate-parse.test.mjs` |
| 18 | [Plan-discovered check commands](./18-plan-discovered-checks.md) | The VERIFY verdict stops resting on a self-report by default: PLAN emits the project's test/typecheck/lint commands as an `agentic-checks` block inside its `### Verification` subsection, the driver runs them and their exit codes bind the verdict. Frozen at plan time (the block is text in the task file, so every iteration checks identically) and capped by the stage's own bash allowlist, which is the boundary rather than the human plan gate — task files are repo content. No manifest gains a command table: one would be wrong for every repo it did not anticipate, and wrong here routes to `onError`. Brings the per-check timeout this made reachable | `workflow/discovered-checks.ts`, `commandAllowed` completing the twin in `task/write-backstop.ts`, `discoverChecks` on `StageDefSchema`, `checksFor`/`configuredChecks`/`discoverChecksFor` + `checkTimeoutMinutes` in `config.ts`, `discoveringStage` + compose tail in `workflow/engine.ts`, the `runChecks` timeout + `ShellPromise.timeout`, `runStageChecks` in both hosts; `discovered-checks.test.ts`, `checks.test.ts`, `config.test.ts`, `schema.test.ts`, `engine.test.ts`, shared allowlist vectors across `write-backstop.test.ts` + `check-stage-guard.test.mjs` |
| 19 | [The gate asks what comes next](./19-gate-follow-up-questions.md) | The three lifecycle questions — approve this draft, plan it now, gate this plan — no longer depend on which path the human took. `GateResult.data` now names the gate crossed (`task`/`plan`/`ship`) and its id, so the Claude/Qwen gate hook can hand the turn back after a TASK gate instead of blocking it, and inject the "plan it now?" imperative itself rather than hoping the verb prose is followed; the same ask rides on the MCP tools' `next` so the tool-call path cannot disagree with the typed-verb path. OpenCode gains `workflow_gate`/`workflow_plan` — an ask whose yes-branch it can actually execute — both refusing a call from inside a running loop so a stage agent can never approve its own task. Also fixes a live Qwen bug: the gates' `next:` strings hardcoded `AskUserQuestion`, a tool that host does not have | `data.gate`/`data.id` in `workflow/gate.ts`, `askTool` on both dialect tables, `hooks/gate-ask.mjs` + `continueOnGate` in `gate-parse.mjs` + the conditional arm in `gate-command.mjs`, `okGate` on the three approve tools, the `approve|plan` marker in `prompts/verbs/engineering.md`, `workflow_gate`/`workflow_plan` + `refuseIfDriven` in the OpenCode driver; `gate.test.ts`, `gate-ask.test.mjs`, `gate-result.test.mjs`, `gate-parse.test.mjs`, `gate-command.test.mjs`, `dialect.test.mjs`, `verb-slice.test.mjs`, `driver.test.ts`, `impl.test.ts` |
| 20 | [Total hooks on the OpenCode host](./20-total-command-hooks.md) | Every first engineering-verb invocation in the OpenCode TUI vanished — no turn, no toast, no log line — and the retry "worked". opencode's `Plugin.trigger` awaits `command.execute.before` with no try/catch and the SDK's fetch back into the server disables its timeout, so a hook that rejects or hangs kills the command before `Session.prompt`; the plugin's hook had ~60 unguarded lines before its dispatch `try`, and the one-shot guards (`reportAgentModelsOnce`, `reconcileOnce`) set their flags before their unguarded awaits, so attempt 1 died inside them and attempt 2 skipped them. Now the hook is total: one try around everything after the prefix match (the catch writes `failurePrompt` — the only channel a dead command has), `log` never rejects and is time-boxed, the per-command config read and the startup sweep are time-boxed (`reconcileTimely` degrades to a toast rather than discarding a gate move that already landed), toasts are fire-and-forget, and the `event` hook is guarded | `withTimeout`/`CONFIG_READ_TIMEOUT_MS`/`LOG_TIMEOUT_MS`/`RECONCILE_TIMEOUT_MS`, the total `log`, `reconcileTimely`, the widened command-hook try, the guarded `event` hook, and the flag-first `warnIgnoredUserConfigOnce`, all in `plugins/opencode/src/impl.ts`; `impl.test.ts` |
| 21 | [A gate move that cannot hang forever](./21-bounded-gate-shell.md) | Answering the task gate's "Approve?" in the OpenCode TUI made the model call `workflow_gate`, and the tool never returned — task file already moved, tool call stuck at `running`, the only exit killing opencode (105s and an ESC the day before). A gate move had no deadline anywhere: OpenCode imposes none on a tool's `execute`, and Bun's `$` has no timeout. Three layers now: the repo's only literal shell glob (`rm -f <stamp> <stamp>.tmp-*`, whose half-quoted token expanded on a WSL `/mnt/c` tree) becomes `find … -maxdepth 1 -name ${pat} -delete` with the pattern escaped, guarded repo-wide by a TypeScript-parsed check; gate verbs get a 60s-per-command `$` resolving exit 124, which core reads as an ordinary failed command so the move still reports and only its bookkeeping is skipped; and every model-callable tool answers — the gate tools in words (retry invited only where `alreadyDone` makes it a no-op), the verdict tools by throwing. Toasts on the gate paths stop being awaited, and `approveAny` is bracketed by log lines so the next stall names its own frame | `sweepStampTemps` in `packages/core/src/claim-marker.ts`, `plugins/opencode/src/bounded-shell.ts` wired through `gateCtx`, `withinDeadline` + the four tool executes in `plugins/opencode/src/impl.ts`, `report`/`gateFromAgent` in `plugins/opencode/src/workflow/driver.ts`; `bounded-shell.test.ts`, `claim-marker.test.ts`, `driver.test.ts`, `scripts/shell-glob.test.mjs` |
| 22 | [A proxy prefix re-expresses the allowlist](./22-command-prefix-allowlist.md) | A command-rewriting proxy (rtk-style) mutates every bash command before either host evaluates permissions, so the documented remedy was one blanket `"rtk *"` extra — which accepts `rtk npm publish` as readily as `rtk npm test`. `bashAllowlistPrefix` derives `<prefix> <glob>` from the globs a stage ALREADY declares instead, granting nothing new: OpenCode reads each sentinel-guarded agent's own `allow` keys as the source (no bootstrap manifest read, user-added kinds covered), Claude/Qwen stamp the prefixed list into the marker. It also fixes a live hole the allowlist can never cover: `isGitPushViolation`/`isGithubPrMutation`/`isFindMutation` anchor on the BARE tool name, so `rtk git push --force origin main`, `rtk gh pr merge 3` and `rtk find . -delete` were classified as no violation — every segment is now classified raw AND with one prefix hop stripped, the prefixes riding the marker as `bashPrefix` because a bundled hook reads neither config nor manifest. Verb RENAMES (`cat x` → `rtk read x`) stay an extras job; no rename table is baked into core | `bashAllowlistPrefixes`/`withCommandPrefixes`/`stripCommandPrefix` in `packages/core/src/config-layers.ts`, `bashAllowlistPrefix` in `config.ts`, prefix-aware `chained*` in `task/write-backstop.ts` + its `hooks/src/allowlist.mjs` twin, `applyBashAllowlistConfig` in `plugins/opencode/src/impl.ts`, `bashPrefix` on the marker in `mcp-server/src/server.ts` + `check-stage-guard.entry.mjs`, admission in `workflow/discovered-checks.ts`; `config-layers.test.ts`, `write-backstop.test.ts`, `discovered-checks.test.ts`, `impl.test.ts`, `server.test.ts`, `check-stage-guard.test.mjs`, `allowlist-prefix.test.mjs` |
| 23 | [The plan gate asks, on every host](./23-plan-gate-ask.md) | Plan 19 gave the TASK gate a harness-emitted follow-up; the PLAN gate — the one where a human must actually read something first — still parked a plan and told the user to type `approve <id>`. Two different failures: OpenCode has no turn to ask in (`plan <id>` returns before its drive starts, so the park lands after that turn ended — toast only), while Claude/Qwen ask only in prose inside `workflow_advance`'s `next` string, which is the data an orchestrator skips. Now OpenCode originates the TURN it cannot originate the question in — one unawaited `session.prompt` fired after the `finally`, where the session is free of the drive — armed by `askOnPark` on the work item (set only in `claimForPlan`, i.e. `plan`/`workflow_plan`/the replan chain, never a watcher) and conditioned on `drive()` returning a park, so no cleanup path can leak a dialog into an unattended worker; `workflow_replan` is added so the Replan option has a tool behind it. Claude/Qwen get a PostToolUse hook that re-emits the same ask as `additionalContext`, sharing one writer with the task gate and failing open on every uncertainty | `askOnPark`/`planParkNextStep`/`promptPlanGateAsk`/`replanAndChain`/`replanFromAgent` in `plugins/opencode/src/workflow/driver.ts`, `workflow_replan` in `plugins/opencode/src/impl.ts`, `planParkAsk` in `plugins/claude/hooks/gate-ask.mjs` + `plugins/claude/hooks/plan-gate-ask.mjs` (both `hooks.json`, `scripts/build-hooks.mjs`); `driver.test.ts`, `plan-gate-ask.test.mjs`, `gate-ask.test.mjs` |
| 24 | [The plan gates see what the plan actually buys](./24-plan-gate-check-preview.md) | A plan whose `agentic-checks` fence was malformed or fully off-allowlist parked clean, got approved, and VERIFY silently ran zero checks — the refusals lived in a `log("warn")` nobody watches. Now the park note + park message carry a pure fire-time forecast (`N admitted for VERIFY` / `NONE admitted (…)` / `no agentic-checks block` — never a veto, per design 18), the fire records the ground truth (`checksSource`/`checksRefused` sample fields + a once-per-run audit note when the outcome would otherwise be silent), `approvePlan` warn-notes a hand-edited plan missing `### Verification` or carrying stacked headings, and the plan-author persona/skill adopt the contract's own section vocabulary | `previewDiscoveredChecks`/`hasChecksFence`/`discoveringStage` in `packages/core/src/workflow/discovered-checks.ts`, the forecast in `runPark` (`workflow/terminal.ts`), caveats in `approvePlan` (`workflow/gate.ts`), sample fields in `workflow/metrics.ts` + `metrics-file.ts`, host provenance in both `runStageChecks`; `discovered-checks.test.ts`, `terminal.test.ts`, `gate.test.ts`, `metrics-file.test.ts` |
| 25 | [Every replan channel carries substance](./25-cap-context-threading.md) | A cap-tripped run discarded everything it learned (`clearState`) and the next PLAN re-planned blind; a bare `replan <id>` rendered `{{#replan}}` as nothing; a claim-held replan dropped the typed reason; no writer bounded reason length. Now a non-transient stop appends a parseable attempts digest (`Run stopped — attempts: …`) that `replanTask` fuses into the rejection reason — design 10's channel, no new state; a reasonless pending rejection threads a fallback via `replanFor` (shared by both entry builders); `oneLineReason` clamps at 1200; the claim-held refusal echoes the reason back | `stopContextNote`/`extractStopContext`/`pendingPlanRejection`/`replanFor` in `packages/core/src/task/store.ts`, `attemptsDigest` in `workflow/terminal.ts`, the fuse/clamp/echo in `workflow/gate.ts`, threading in `workflow/orchestrate.ts` + `source/backlog.ts`, the ledger sentence in `workflows/engineering/stages/plan.md`; `store.test.ts`, `terminal.test.ts`, `gate.test.ts`, `engine.test.ts` |
| 26 | [The task gate validates what it approves](./26-task-gate-validation.md) | `approveTask` checked only location + the epic refusal, while the authoring path (raw Write tool) had no secret screen and a criteria-less draft approved silently. Now the gate refuses a draft that scans as carrying a secret (hub-parity `redact()`, naming the patterns and the `retask` route) and warn-notes an empty acceptance list (`data.acceptanceMissing`) — warn, never refuse: criteria-less chores are legal | `approveTask` in `packages/core/src/workflow/gate.ts`; `gate.test.ts` |
| 27 | [Plan quality gets its own numbers](./27-plan-quality-metrics.md) | `capTripRate` was the only plan-quality proxy, conflating bad plans with hard tasks and flaky suites, while the evidence sat unaggregated on disk. The Metrics tab now carries replan rate (run files with ≥2 plan-stage passes), contract refusals (matched against the park gate's exported refusal consts, so writer and matcher cannot drift), and check-command provenance by source + refused total (per stage×iteration firing, deduped under fan-out) | `PlanQualityStats`/`DiscoveryStats` in `packages/hub/src/shared/api.ts`, `planStats`/`discoveryStats` in `packages/hub/src/server/metrics/aggregate.ts`, the chip row in `web/metrics/MetricsTab.tsx`, `PARK_NO_PLAN_WHY`/`PARK_NO_VERIFICATION_WHY` in `packages/core/src/workflow/terminal.ts`; `aggregate.test.ts` |
| 28 | [`claim` takes a task id](./28-claim-by-id.md) | A backlog with several build-ready tasks had no way to say "build THIS one": bare `claim` walks the pools by priority, and `plan <id>` refused build-ready ids while pointing back at the very walk that cannot target them. The machinery was half-built — the Claude host's `workflow_start({id})` had no verb routed to it, and the OpenCode driver's `start-task` pending had consumer arms with no producer. `claim [id]` closes the circle on every host: a build-ready `in-progress/` id starts at BUILD, an approved `queued/` id runs its PLAN pass (same `claimForPlan` path as `plan <id>`, ask-on-park included), any other folder is refused with the verb to use instead | `startTaskById` + the claim branch in `plugins/opencode/src/workflow/driver.ts`, the `claim [id]` block in `prompts/verbs/engineering.md` (regenerated into both hosts' `verbs/`), `argument-hint` on all three hosts; `driver.test.ts` |
| 29 | [Doctor reads the deny log](./29-deny-telemetry.md) | Allowlist starvation was diagnosed by transcript archaeology four separate times (mvn/gradle argv order, JS workspace selectors, a rewriting proxy's prefix) — the remedy keys existed, the diagnosis did not. The enforcement seams now append every refused bash command to `<tasksDir>/runs/.deny-log.jsonl` (Claude/Qwen: the check-stage guard's block arm; OpenCode: an observe-only `permission.ask` hook, best-effort), and `doctor` aggregates it per kind+stage+command with a mechanically derived suggestion — `bashAllowlistPrefix` when the wrapped command is already allowed (proof required), else the narrowest `bashAllowlistExtra` glob; `doctor fix` clears the reported log. A report, never an applied config change — an extra widens the T2 boundary, so breadth stays the operator's call | `packages/core/src/workflow/deny-log.ts`, `noteDeny` in `plugins/claude/hooks/src/deny.mjs` + the block arm in `check-stage-guard.entry.mjs`, the `permission.ask` observer in `plugins/opencode/src/impl.ts`, doctor wiring in `plugins/opencode/src/workflow/driver.ts` + `mcp-server/src/server.ts`; `deny-log.test.ts`, `deny.test.mjs` |
| 30 | [`--auto-plan` thins the plan gate per task](./30-auto-plan.md) | Three human gates per task means the plan review is a rubber stamp for chore-sized work, and every blunt remedy is worse (a repo-wide skip thins the gate for risky tasks too; "continue if trivial" prose puts control flow on the model's mood). `approve <id> --auto-plan` is a per-task, explicit task-gate opt-in written into the schema'd frontmatter; when that task's plan parks, the gate is crossed deterministically — OpenCode's `autoAdvanceParkedPlan` approves and queues the BUILD drive (suppressing the plan-gate ask), Claude/Qwen's `runPark` approves server-side and omits `gate` from its descriptor so the ask hook stays silent. `replan` clears the flag, a fresh plain approve clears it, the ship gate is never automated, and a failed auto-approve falls back to the ordinary human gate | `autoPlan` in `packages/core/src/task/schema.ts`, `AUTO_PLAN_FLAG`/`parseGateOptions` in `config.ts`, `writeAutoPlanFlag` + `approveTask`/`approveAny`/`replanTask` in `workflow/gate.ts`, `autoAdvanceParkedPlan` in `plugins/opencode/src/workflow/driver.ts`, `runPark` + `workflow_approve` in `mcp-server/src/server.ts`; `schema.test.ts`, `config.test.ts`, `gate.test.ts`, `driver.test.ts` |
| 31 | [Terminal events can push a notification](./31-terminal-notifications.md) | The gates go quiet when nobody watches the terminal: a park is a toast in scrollback, a watch worker piles finished runs into in-review/ silently, a stopped loop waits on a human who doesn't know it stopped. `notifyCommand` (user-scope, SHELL-BEARING — repo layer drops it with the existing per-key warning) fires after every terminal event as `sh -c <command>` with `AW_EVENT`/`AW_KIND`/`AW_TASK`/`AW_MESSAGE` in env, every value an escaped interpolation; `notifyEvents` narrows the set (four literals, repo-settable). One choke point — `notifyTerminal` wraps core's `runTerminal`, which every host and kind already routes through — bounded at 10s and best-effort: a slow or failing notifier warns and never changes the outcome | `notifyCommand`/`notifyEvents` in `packages/core/src/config.ts` + `workflow/state.ts`, `SHELL_BEARING_KEYS` in `config-layers.ts`, `notifyTerminal`/`NOTIFY_TIMEOUT_MS` in `workflow/terminal.ts`; `terminal.test.ts` |
| 32 | [A plan may not name a dependency it did not read](./32-plan-verified-dependencies.md) | PLAN has no shell and no network on any host (`bash: deny`; Claude/Qwen grant only Read/Grep/Glob/Write), so every version in a plan came from PUBLIC-registry memory — which on a repo pointed at an internal mirror is an answer to a different question, and BUILD only found out at install time, an iteration late. A composed `dependencyContractBlock` asks for reuse-first ordering (lockfile → stdlib → new), a version READ and cited `file:line` rather than recalled, the registry the repo actually resolves from (`.npmrc`/`settings.xml`/`pip.conf`, read not tabulated), and an explicit "could not establish" where it could not; the machine-readable half is an `agentic-deps` fence in a conditional `### Dependencies` section, forecast onto the park note, the park message and `approvePlan`'s caveats. Nothing probes anything — a driver-run registry check was designed and abandoned (a PLAN `bashAllowlist` is not admission-only: `[]` means unrestricted, and a non-empty marker list narrows every Bash call in the human's own session; `runPark` has no bounded shell; a park-time green would launder the human's interactive env into fact). BUILD closes it from the other end: a plan-named dependency that will not resolve is a PLAN defect routed to `build.onError`, never a substitution | `packages/core/src/workflow/declared-deps.ts`, the compose tail in `workflow/engine.ts`, the park forecast in `workflow/terminal.ts`, the caveat in `workflow/gate.ts`, `prompts/agents/workflow-plan-author/body.md` + `skills/planning-and-task-breakdown/SKILL.md`, `workflows/engineering/stages/build.md`; `declared-deps.test.ts`, `terminal.test.ts`, `gate.test.ts`, `engine.test.ts` |

| 33 | [The ship gate leads with the diff it gates](./33-ship-gate-diff-aid.md) | The gate's whole contract is "review the branch diff", and nothing computed it: the done note named a branch and stopped, the Claude host asked the MODEL to derive the summary, the hub had no diff view, and no `--shortstat` existed in product code. Now `runDone` computes a validated `git diff --shortstat <base>...<branch>` while the run still knows its range and threads it three ways: a trailing `; diff: …` clause on the done note (LAST, so its commas cannot move the comma-terminated branch/base fields ahead of it), `diffstat`/`diffCmd` on the `TerminalReport` (the OpenCode toast and the Claude ship-gate descriptor lead with numbers and the literal command), and `extractRunDiffstat` reads it back for the hub's review cards. Every failure degrades to the pre-clause note, byte-identical | `diffShortstat` in `packages/core/src/workflow/git.ts`, the probe + clause in `runDone` (`workflow/terminal.ts`), `RUN_DIFF_PREFIX`/`extractRunDiffstat` in `task/store.ts`, the toast in `plugins/opencode/src/workflow/driver.ts`, the descriptor in `plugins/claude/mcp-server/src/server.ts`, `ReviewItem.branch`/`diffstat` in `packages/hub/src/shared/api.ts` + `server/routes/review.ts` + `web/review/ReviewQueue.tsx`; `git.test.ts`, `store.test.ts`, `terminal.test.ts` |
| 34 | [A passing review's suggestions reach the human](./34-review-suggestions-to-ship-gate.md) | `verdictFeedbackBlock` filters findings to blocking on purpose (a suggestion must not burn a rebuild), but that filter was the only exit from a check stage — so a passing REVIEW's non-blocking findings survived only in the metrics sidecar, a cross-run aggregate nobody reads at the ship gate. `suggestionFindings` (the filter's exact complement, capped at 10) rides the done ACTION — never the state: it matters only at this terminal, and a persisted field would need a schema key zod otherwise strips — and `runDone` writes one redacted, clamped `Review suggestions (N) — …` audit note before the done note (which stays the trail's newest line) while the report carries them un-flattened for both hosts to relay, explicitly marked non-blocking. The BUILD feedback seam stays clean, pinned by test | `suggestionFindings` in `packages/core/src/workflow/verdict.ts`, the done arm of `advance` in `workflow/engine.ts` + `Action` in `workflow/state.ts`, the note + report in `runDone` (`workflow/terminal.ts`), the toast/descriptor in `plugins/opencode/src/workflow/driver.ts` + `plugins/claude/mcp-server/src/server.ts`; `verdict.test.ts`, `engine.test.ts`, `terminal.test.ts` |
| 35 | [Status says what to type next](./35-status-next-actions.md) | `summarizeBacklog` rolls the backlog into seven actionable id lists and the hosts rendered the counts while swallowing the verbs — OpenCode hinted at two of seven, the Claude host at none, and that host also called `summarizeBacklog` without the claim ids, so `claimHeld` was always empty and claim-held tasks misreported as claimable. `nextActions` is one pure renderer beside the summary — one verb-bearing line per non-empty list, human wait-gates first, ids elided past 5, the command prefix a parameter — shared by both hosts so the state→verb mapping cannot drift; the Claude host's status now passes `listClaimIds` and returns the `nextActions` array | `nextActions` in `packages/core/src/task/store.ts`, the hint loop in the status verb (`plugins/opencode/src/workflow/driver.ts`), the `claimedIds` fix + `nextActions` field in `workflow_status` (`plugins/claude/mcp-server/src/server.ts`); `store.test.ts` |

| 36 | [An epic's progress is reported, and its close-out is offered](./36-epic-progress-closeout.md) | A slice set's tracking epic sits in `draft/` for good and its documented close ("abandon it by hand once every child has shipped") was surfaced nowhere — `epicSiblings` had one consumer (the task-gate walk), `shipTask` never touched epics, and nothing counted a set's progress. Now `shipTask` reports the set best-effort after the move (`Epic <id>: 1/3 slices shipped; still open: …`, and on the last slice `all N slices shipped — abandon <id> closes the tracker`, mirrored in `data`), `summarizeBacklog` gains per-epic `EpicProgress` rows derived purely from the `epic:` links (emptied trackers yield none; the Claude status carries it for free), and `nextActions` names the close-out on both hosts. Abandoned slices shrink the total on purpose — abandoning is the documented way to cut scope | `epicShipOutcome` + the ship tail in `packages/core/src/workflow/gate.ts`, `EpicProgress`/`epicProgress`/the `epics` key + close-out line in `task/store.ts`, `ACTIVE_STATUSES` exported; `gate.test.ts`, `store.test.ts` |
| 37 | [One seam answers "what config is in force, and why not mine"](./37-effective-config-view.md) | The repo-layer drops lived as three private functions warning into a log nobody keeps; no CLI printed the resolved config; the hub's "effective" view merged RAW layers, showing a repo-layer `stageChecks`/`worktreeSetup`/`ado.organization` as in effect when the loop ignores it; and `readRawConfigLayers` stripped only top-level shell keys while its doc claimed loadConfig parity. Now `droppedRepoKeys`/`sanitizeRepoLayer` are the one pure seam every surface reads — `loadConfigWith`'s warnings (texts preserved verbatim), the hub's effective/provenance/issues views (plus a `droppedRepoKeys` response field rendered as "Set here, ignored at runtime"), `readRawConfigLayers` (now the FULL drop set), and `doctor config` on both hosts, which returns `effectiveConfigReport`: layer paths, dropped keys, and the host's actually-loaded config masked by `maskConfigSecrets` | `droppedRepoKeys`/`sanitizeRepoLayer`/`maskConfigSecrets`/`effectiveConfigReport` in `packages/core/src/config-layers.ts` (re-exported by `config.ts`, whose three drop functions are deleted), the sanitized merge + `droppedRepoKeys` in `packages/hub/src/server/routes/config.ts` + `shared/api.ts` + `web/config/ConfigEditor.tsx`, the `doctor config` branch in `plugins/opencode/src/workflow/driver.ts` + `workflow_doctor`'s `config` input in `plugins/claude/mcp-server/src/server.ts`; `config-layers.test.ts`, `config.test.ts` |
| 38 | [Check-admission refusals join the deny log](./38-check-refusal-telemetry.md) | Design 29's telemetry covered only the agent seam: a plan-named discovered check the stage refused was a warn line plus a `checksRefused` metric that counted `warnings.length` — parse issues, refusals, and missing binaries conflated — so the exact starvation it existed to end kept a silent seam. `ResolvedChecks` gains structured `refused` (each `RejectedCheck` now carries its `command`), both hosts append `source: "check"` deny-log entries at their `resolveStageChecks` call sites (absent source reads as agent — old entries unchanged), `DenyFinding.fromChecks` makes doctor's report say "(a plan-discovered check)", and `checksRefused` counts admission refusals alone. Missing binaries stay out — an environment fact no allowlist edit answers | `refused` + `RejectedCheck.command` in `packages/core/src/workflow/discovered-checks.ts`, `source`/`fromChecks` + the report tag in `workflow/deny-log.ts`, the entry writers + honest metric in `plugins/opencode/src/workflow/driver.ts` and `plugins/claude/mcp-server/src/server.ts`; `discovered-checks.test.ts`, `deny-log.test.ts` |

| 39 | [`init` scaffolds a repo on day one](./39-init-verb.md) | Nothing set a repo up: status folders appear lazily, the repo config is a filename to hand-write, and the backlog's git-exclude happens on the first claim — fine for the loop, invisible to the human onboarding a new repo (the installers deliberately never touch one). `initRepo` creates the `tasksDir` status folders, writes a safe-key `.agentic-workflow.json` when none exists, and runs the claim path's own `ensureExcluded` under `ignoreBacklog`; OpenCode handles the verb deterministically like `doctor`, Claude/Qwen get `workflow_init`. Idempotent and NEVER overwrites (a second run reports `kept` and issues no write — test-pinned); the skeleton carries `tasksDir` + `maxIterations` only, nothing shell-bearing; no placeholder files inside `tasksDir`, which `auditBacklog` would read as damage | `initRepo`/`initConfigSkeleton` in `packages/core/src/workflow/init.ts`, the `init` arm in `plugins/opencode/src/workflow/driver.ts`, `workflow_init` in `plugins/claude/mcp-server/src/server.ts`, the verb block in `prompts/verbs/engineering.md` + host hints/routers; `init.test.ts` |
| 40 | [`approve --all` batches the task gate, and only the task gate](./40-approve-all.md) | `new` drafts a slice set the human reviews in one sitting — and then the gates make them type N approves (a bare `approve` over several candidates is deliberately an ambiguity refusal). `--all` parses in `parseGateOptions` (every host's parser inherits it; the bundled hooks forward dash-words untouched), routes first in `approveAny`, and lands in `approveAllTasks`: every reviewed draft, priority order, epics excluded, `approveTask` per child — per-child refusals (the secret scan) don't stop the walk and ride the message as a `warning`. Task gate ONLY by construction (plan/ship stay one-at-a-time — those need a human to have read something specific); an id beside `--all` is refused at parse; the batch result carries no `gate`/`id` keys, so design 19's fail-safe arm keeps both hosts' follow-up asks quiet with no new ask machinery | `ALL_FLAG` + the parse in `packages/core/src/config.ts`, `approveAllTasks` + the routing in `workflow/gate.ts`, the `all` argument on `workflow_approve` + the CLI arm in `plugins/claude/mcp-server/src/server.ts`, the OpenCode approve arm in `plugins/opencode/src/workflow/driver.ts`, the flag bullet in `prompts/verbs/engineering.md`; `config.test.ts`, `gate.test.ts` |
| 41 | [Auto-plan declines to cross past plan caveats](./41-auto-plan-caveat-guard.md) | Both hosts' `--auto-plan` crossings dropped the caveats `approvePlan` computes for the manual gate, so a plan with no `### Verification` subsection (the defect whose cost lands a whole iteration later — no discovered checks will run) crossed silently, seen by no one. The caveat list is now an exported pure seam (`planCaveats`) judged by both auto-cross arms BEFORE approving: any caveat declines the automatic crossing — the plan stays parked, the flag stays armed, the outcome names the caveats and the way through (a manual approve still crosses; replan still rejects). Fail toward human review; a contract-clean plan's rubber-stamp path is byte-identical, test-pinned on both the chain and no-chain arms | `planCaveats` in `packages/core/src/workflow/gate.ts` (approvePlan now calls it), the decline arms in `autoAdvanceParkedPlan` (`plugins/opencode/src/workflow/driver.ts`) and the park arm of `workflow_advance` (`plugins/claude/mcp-server/src/server.ts`); `gate.test.ts`, `driver.test.ts` |
| 42 | [A gate move cannot hang or lose its envelope on the model hosts](./42-bounded-gate-model-hosts.md) | Design 21 stopped at OpenCode, and the hang class is host-agnostic: a `workflow_approve`/`workflow_ship` on a slow tree left the MCP call `running` forever — and the gate hook was worse, because Claude Code kills a command hook at its own 60s deadline and drops the WHOLE envelope (GateResult and block), turning a slow gate into a silent double-dispatch. The MCP server's `gateCtx` now hands core a 60s-per-command bounded shell through the shim's own child-killing `.timeout` (exit 124, which core reads as an ordinary failed command — the move still reports); the hook's `spawnSync` gets a 50s deadline and `decideGateOutcome` gains a distinct ETIMEDOUT arm that BLOCKS, fail closed, naming what to check before retrying. The plain `sh` stays unbounded (checkpoints, worktree setup, runChecks carry their own regime) | `GATE_SHELL_TIMEOUT_MS` + `boundedGateSh` in `plugins/claude/mcp-server/src/server.ts`, the spawn `timeout` in `plugins/claude/hooks/gate-command.mjs`, the ETIMEDOUT arm in `hooks/gate-result.mjs` (Qwen bundle regenerated); `gate-result.test.mjs` |
| 43 | [PLAN sees the allowlist its discovered checks are judged by](./43-plan-sees-allowlist.md) | `checkDiscoveryBlock` demanded allowlisted commands and never showed the globs — PLAN has no bash and no way to probe, so a monorepo plan naming `pnpm --filter web test` in a perfectly readable shape was refused at admission an iteration later behind one warning line; designs 24/29/38 all instrument the failure after the fact, and write time was the only uninstrumented seam. `discoveryAllowlist` (kept beside `previewDiscoveredChecks` so it cannot drift from admission's arguments) composes the consumer's manifest globs + `bashAllowlistExtra` — bare shapes only, prefix twins deliberately omitted — and the discovery block renders them, clamped by whole patterns with the elided count named. Config-less composes the same list from the manifest alone, keeping the unset-knob pin and the hub preview's byte-for-byte guarantee (the preview passes it explicitly) | `discoveryAllowlist` + `allowlistSentence` in `packages/core/src/workflow/discovered-checks.ts`, the `discoverGlobs` parameter through `composeStagePrompt`/`composePromptWithStats` (`workflow/engine.ts`, which re-exports the helper), the preview call in `packages/hub/src/server/routes/kinds.ts`; `discovered-checks.test.ts`, `engine.test.ts` |
| 44 | [Status sees loops driven by other processes](./44-cross-process-status.md) | Both hosts' status answered from their own process alone (`getWorkflow` / the in-memory `active`), so a watch worker in another terminal driving task X read as "no active loop" here — inviting a competing claim that bounced off refusals status never foreshadowed, though the oracle (`taskDrivenByStageMarker`) already existed for recover/doctor. `liveStageMarkers` reports every host's LIVE marker (task, stage, kind, deadline, pid) under the exact same liveness rule — both readers now share one internal, so the oracles cannot drift. OpenCode's status appends a `driven elsewhere: <task> @ <stage> (<host> pid N, deadline in Mm)` segment; `workflow_status` gains `drivenElsewhere`, each entry saying the gate verbs will refuse while that loop is live. Each host filters its own pid so the driving session's loop is never reported twice | `LiveStageMarker`/`liveMarkerFor`/`liveStageMarkers` in `packages/core/src/workflow/stage-marker.ts`, the status segment in `plugins/opencode/src/workflow/driver.ts`, the `drivenElsewhere` field in `plugins/claude/mcp-server/src/server.ts`; `stage-marker.test.ts` |
| 45 | [Plan-review comments share the reason budget](./45-comment-reason-budget.md) | The hub drawer fused per-line comments into one replan reason unbounded, while core clamps every reason at `REPLAN_REASON_MAX` (1200) — so three average anchored comments composed past the clamp and `oneLineReason`'s ellipsis ate the TAIL comments whole: the drawer invited comments it silently never delivered to the next PLAN pass, the exact vague-replan failure it was built to fix. Composition is budget-aware now — a comfortable form is byte-identical to before; past the budget the note allotment divides evenly (floor 40) so every comment survives clipped, anchors always whole — and the send bar shows a `reason N/1200` meter that says when notes are squeezed. The budget constant is declared web-side (browser bundle; core is node-flavoured) and pinned equal to core's by test | `REASON_BUDGET`/budget-aware `composeReason`/`reasonStats` in `packages/hub/src/web/monitor/comments.ts`, the meter in `web/monitor/TaskReview.tsx`; `comments.test.ts` (pins `REASON_BUDGET === REPLAN_REASON_MAX`) |
| 46 | [The stall rule, and findings that know they are repeats](./46-stall-rule-and-finding-ids.md) | The only bound on a run was `maxIterations`, judged by count alone: the attempts ledger was rendered for the prompts to read and nothing compared two attempts, so a VERIFY failing on the same criteria and the same critical finding three times running burned every iteration before the cap said "the plan is likely wrong" — a conclusion the second identical pass had proved. And `AxisFinding` had no identity, so the rebuild seam re-emitted every blocking finding verbatim each iteration. Every counted attempt now carries a structural fingerprint (failed criteria + blocking finding ids, never the reason prose) and a counted fire may set `stallAfter: N` — engineering stops both check stages at 2 identical failures, cap checked first; a reason-only FAIL has no fingerprint and never stalls. Blocking findings carry a stable `findingId` (axis + severity + normalized location, line kept), and one the ledger already holds renders as a REPEAT naming the iteration it was last raised in | `findingId`/`blockingFindingIds`/`failureFingerprint` and the `prior` map on `verdictFeedbackBlock` in `verdict.ts`; `withAttempt`/`priorFindings`/`trailingSameFailures` + the stall arm in `engine.ts`; `stallAfter`/`stallMessage` in `manifest/schema.ts`; `fingerprint`/`findings` on `AttemptRecord` (`state.ts`, declared in `persist.ts`); `engine.test.ts`, `verdict.test.ts`, `schema.test.ts`, `persist.test.ts` |
| 47 | [The automatic checkpoint screens what it sweeps](./47-checkpoint-screen.md) | `commitAll` — every work stage's checkpoint on both hosts — was a `git add -A` excluding only the backlog dir and the lockfiles, so a `.env` a setup script wrote, a `.pem` an install dropped, a `credentials.json` from a test harness, or a downloaded fixture rode the feature branch into the PR's permanent history; design 05's redaction covers the durable TEXT artifacts and never looked at committed code. `screenCheckpoint` now runs INSIDE `commitAll` (so no checkpoint site can skip it): it reads the raw porcelain, refuses secret-shaped basenames (`CHECKPOINT_SECRET_BASENAMES`: dotenv bar the doc conventions, keys/keystores, SSH private keys, credential files, tfstate) and blobs over `CHECKPOINT_BLOB_MAX` (5 MiB) as `:(exclude,literal)` pathspecs on the same add, and returns `{ committed, screened }` so every host site warns which paths it kept out and how to commit one explicitly. Narrows the AUTOMATIC sweep only, and fails toward the sweep | `screenCheckpoint`/`sweptPaths`/`secretShapedPath`/`screenedWarning`/`runRaw` and the `CheckpointResult` return in `workflow/git.ts`; the four checkpoint sites in `plugins/opencode/src/workflow/driver.ts` and `plugins/claude/mcp-server/src/server.ts`; `git.test.ts`, `current-branch.git.test.ts` |
| 48 | [A check stage can say the plan is wrong](./48-plan-defect-arm.md) | Only BUILD could report an unimplementable plan (`workflow_blocked`, refused to check stages on purpose) — yet VERIFY and REVIEW are where a wrong plan is DISCOVERED, and all they could do was FAIL, which re-fires BUILD, so the run rebuilt against a plan that could not pass until the cap tripped. `VerdictRecord.planDefect` + a `planDefect` argument on `workflow_verdict` on both hosts, pinned by `admitVerdict` to a FAIL with a reason (a PASS is a contradiction, a bare flag tells the next planner nothing) and kept through a merge; a check stage's `onPlanDefect` transition arm, validated like every other, taken on such a FAIL — engineering stops both check stages naming `replan <id>`, no iteration spent, the attempt on the ledger so `runStop`'s stop-context note carries the reason into the replan's PLAN pass. The contract block says when: never for a defect the build can fix, never with PASS, never to escape a hard task | `planDefect` on `VerdictRecord`, `planDefectIssue` + merge in `admitVerdict`, the PLAN DEFECT paragraph in `verdictContractBlock` (`verdict.ts`); `onPlanDefect` in `manifest/schema.ts`; the routing + ledger arm in `engine.ts`; the `planDefect` tool argument in `plugins/opencode/src/impl.ts` and `plugins/claude/mcp-server/src/server.ts`; `verdict.test.ts`, `engine.test.ts`, `schema.test.ts` |

Residuals still open: cross-process `index.lock` races and redaction knobs. (Two
entries this list used to carry have since shipped — bash worktree pinning in
`packages/core/src/workflow/worktree-guard.ts`, and metrics export in
`workflow/metrics-file.ts` plus `packages/hub/src/server/metrics/`.) Plan 09 left
persona/skill weight, model-call summarization, and token-accurate budgets
explicitly out of scope. See [`../threat-model.md`](../threat-model.md) for the
current residual risks.

## Conventions every plan follows

- **TDD**: each plan lists the failing tests to write first. The suite must
  stay green with all new config knobs unset (backward compatibility is a
  hard requirement — every feature here is opt-in or purely additive).
- **Purity boundary**: `packages/core/src/workflow/state.ts` and the predicate
  helpers in `packages/core/src/task/store.ts` stay pure. Anything that
  touches the shell, clock, or filesystem lives in `driver.ts`, `git.ts`,
  `store.ts`'s IO half, or a new impure module.
- **Docs are part of done**: each plan ends with the exact docs to update
  (`README.md`, `.opencode/commands/agent-loop.md`,
  `skills/workflow-orchestration/SKILL.md`,
  `skills/task-backlog-management/SKILL.md`, `docs/design/threat-model.md`)
  so the earlier `in-review`-style doc drift doesn't repeat.
